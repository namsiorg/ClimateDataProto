'use strict';

describe('myApp.viewConfig module', function() {

  beforeEach(module('myApp.viewConfig'));

  describe('viewConfig controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewConfigCtrl = $controller('ViewConfigCtrl');
      expect(viewConfigCtrl).toBeDefined();
    }));

  });
});